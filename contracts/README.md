# Soroban contracts

This workspace includes two production-oriented Soroban contracts:

- `license_registry` for managing creators, license terms, and state transitions.
- `royalty_router` for routing royalty events and proving contract-to-contract calls.

The contracts are designed to be deployed to Stellar testnet and upgraded safely with versioned metadata.
